<div class="vyuziti-wrapper">
        {% for media in page.media %}
            <img src="{{ media.url }}" alt="{{ media.title }}" class="vyuziti-image" />
        {% else %}
            <img src="http://placehold.it/750x500" alt="placeholder image" class="vyuziti-image placeholder" />
        {% endfor %}
    <h5 class="vyuziti-heading font md wh">{{ page.title }}</h5>
    {# {{page.content|raw}} zakomentováno, protože se možná dodělají titulky na jednotlivý popis produktů#}
</div>
